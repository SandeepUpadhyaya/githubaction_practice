name: Daily Script

on:
  schedule:
    - cron: '15 9 * * *'  # Runs daily at 09:05 UTC (2:35 PM IST)

jobs:
  run_script:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11.9'
      
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      
      - name: Run script
        run: |
          python app.py
